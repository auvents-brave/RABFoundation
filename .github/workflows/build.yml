name: Build Swift Package on Multiple Platforms

permissions:
  contents: read

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

env:
  SWIFT_VERSION: "6.1"

jobs:
  build-macos:
    name: Build & Test macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - uses: swift-actions/setup-swift@next
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - run: swift --version

      - run: swift build --disable-sandbox

      - run: swift test --disable-sandbox

  build-maccatalyst:
    name: Build macCatalyst
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - uses: swift-actions/setup-swift@next
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - run: swift --version

      - run: |
          xcodebuild \
            -scheme rab-foundation \
            -destination 'platform=macOS,variant=Mac Catalyst' \
            clean build

  build-ios:
    name: Build iOS Simulator
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - uses: swift-actions/setup-swift@next
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - run: swift --version

      - run: xcodebuild -list

      - run: |
          xcodebuild \
            -scheme rab-foundation \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 16e,OS=18.4' \
            clean build

  build-tvos:
    name: Build tvOS Simulator
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - uses: swift-actions/setup-swift@next
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - run: swift --version

      - run: |
          xcodebuild \
            -scheme rab-foundation \
            -sdk appletvsimulator \
            -destination 'platform=tvOS Simulator,name=Apple TV 4K (3rd generation) (at 1080p),OS=18.4' \
            clean build

  build-watchos:
    name: Build watchOS Simulator
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - uses: swift-actions/setup-swift@next
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - run: swift --version

      - run: |
          xcodebuild \
            -scheme rab-foundation \
            -sdk watchsimulator \
            -destination 'platform=watchOS Simulator,name=Apple Watch Ultra 2 (49mm),OS=11.2' \
            clean build

#  build-visionos:
#    name: Build visionOS Simulator
#    runs-on: macos-latest
#    steps:
#      - uses: actions/checkout@v3

#     - uses: swift-actions/setup-swift@next
#       with:
#         swift-version: ${{ env.SWIFT_VERSION }}

#     - run: swift --version

#     - run: |
#         xcodebuild \
#           -scheme rab-foundation \
#           -sdk visionossimulator \
#           -destination 'platform=visionOS Simulator,name=Apple Vision Pro,OS=1.0' \
#           clean build

  build-linux:
    name: Build & Test Linux
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3

      - uses: swift-actions/setup-swift@next
        with:
          swift-version: ${{ env.SWIFT_VERSION }}

      - run: swift --version

      - run: swift build --disable-sandbox

      - run: swift test --disable-sandbox

  build-windows:
    name: Build & Test Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Swift 6.1 on Windows
        uses: compnerd/gha-setup-swift@main
        with:
          branch: swift-6.1-release
          tag: 6.1-RELEASE

      - name: Show Swift version
        run: swift --version

      - name: Build Swift Package
        run: swift build

      - name: Run Tests
        run: swift test

#  build-windows:
#    name: Build & Test Windows
#    runs-on: windows-latest
#    steps:
#      - uses: actions/checkout@v3
#
#      - uses: swift-actions/setup-swift@v1
#
#      - run: swift --version
#
#      - run: swift build --disable-sandbox
#
#      - run: swift test --disable-sandbox

#  build-carton:
#    name: Build & Test for WebAssembly (SwiftWasm)
#    runs-on: ubuntu-22.04

#   env:
#     SWIFTWASM_TAG: latest     # 5.9.2

#   steps:
#     - name: Checkout repository
#       uses: actions/checkout@v3

#      - name: Build Swift Package for WebAssembly
#         run: |
#           docker run --rm \
#             -v "$(pwd):/src" \
#             -w /src \
#             ghcr.io/swiftwasm/carton:${SWIFTWASM_TAG} \
#             carton bundle

#      - name: Run Swift Tests in WebAssembly
#        run: |
#          docker run --rm \
#            -v "$(pwd):/src" \
#            -w /src \
#            ghcr.io/swiftwasm/carton:${SWIFTWASM_TAG} \
#            carton test

  build-wasm:
    name: Build & Test for WebAssembly (SwiftWasm)
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3

      - name: Install Swift SDK for wasm32-unknown-wasi
        run: |
          curl -LO https://github.com/swiftwasm/swift/releases/download/swift-wasm-6.1-RELEASE/swift-wasm-6.1-RELEASE-wasm32-unknown-wasi.artifactbundle.zip
          echo "7550b4c77a55f4b637c376f5d192f297fe185607003a6212ad608276928db992  swift-wasm-6.1-RELEASE-wasm32-unknown-wasi.artifactbundle.zip" | sha256sum --check
          swift sdk install swift-wasm-6.1-RELEASE-wasm32-unknown-wasi.artifactbundle.zip

      - name: Build Swift Package for WebAssembly
        run: |
          swift build --swift-sdk wasm32-unknown-wasi

      - name: Test Swift Package (if tests are available)
        continue-on-error: true  # WASI test support may vary
        run: |
          swift test --swift-sdk wasm32-unknown-wasi
