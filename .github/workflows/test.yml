name: TEST
permissions:
  contents: read
on:
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:

  build-windows:
    name: Build on Windows with Swift 6.2
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download Swift toolchain
        run: |
          curl -L -o swift.zip https://download.swift.org/swift-6.2-release/windows10/swift-6.2-RELEASE-windows10.zip
          mkdir C:\Swift
          tar -xf swift.zip -C C:\Swift --strip-components=1

      - name: Add Swift to PATH
        shell: pwsh
        run: |
          echo "##[add-path]C:\Swift\bin"
          $env:Path += ";C:\Swift\bin"
          [System.Environment]::SetEnvironmentVariable("PATH", $env:Path, [System.EnvironmentVariableTarget]::Process)

      - name: Check Swift Version
        shell: pwsh
        run: |
          swift --version

  build-linux:
    name: Linux
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Check Swift Version
        run: swift --version

      - name: Install dependencies
        run: |
          sudo apt-get -y install libcurl4-openssl-dev

      - name: Install Swiftly
        run: |
          curl -O https://download.swift.org/swiftly/linux/swiftly-$(uname -m).tar.gz
          tar zxf swiftly-$(uname -m).tar.gz
          ./swiftly init --quiet-shell-followup
          # Get Swift binary path and persist it for future steps
          source "${SWIFTLY_HOME_DIR:-$HOME/.local/share/swiftly}/env.sh"
          echo "${SWIFTLY_HOME_DIR:-$HOME/.local/share/swiftly}/bin" >> $GITHUB_PATH

      - name: Check Swift Version
        run: swift --version

  build-mac:
    name: Mac
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Check Swift Version
        run: swift --version

      - name: Install Swiftly
        run: |
          curl -O https://download.swift.org/swiftly/darwin/swiftly.pkg
          installer -pkg swiftly.pkg -target CurrentUserHomeDirectory
          ~/.swiftly/bin/swiftly init --quiet-shell-followup
          . "${SWIFTLY_HOME_DIR:-$HOME/.swiftly}/env.sh"
          hash -r

      - name: Check Swift Version
        run: swift --version
